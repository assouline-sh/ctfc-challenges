# Port Probe
## Misc

a = SSH + WHOIS 
b = Kerberos + HTTP 
c = FTP + HTTPS 
d = Finger + SMTP
(a + b) * (c - d) = x
Format the flag as CTF{x} where x is the value of x

- Find ports for each protocol and do the math


CTF{83647}
